{
  "name": "depth-clock-android",
  "version": "1.0.0",
  "private": true,
  "description": "Depth Clock — Live Wallpaper editor, wrapped for Android with Capacitor",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
